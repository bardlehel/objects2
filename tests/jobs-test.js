import mocha from 'mocha';
import chai from 'chai';

describe('Background Jobs Service Test', function () {
    it('should get Person category when queried with Person properties', function (done) {
        
        done();
    });

    it('should return parents containing Person after adding person categories', function (done) {
        
        done();
    });
});
